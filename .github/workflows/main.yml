name: Build Android APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Status
        run: echo "Build Successful for delivery-tracking-delta.vercel.app"
